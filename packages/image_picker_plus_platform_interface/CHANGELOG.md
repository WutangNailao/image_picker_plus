## 0.0.3

* **BREAKING**: Renamed `XFileWithLocalIdentifier` to `XFileWithMetadata`.
* Added `contentUri` field to `XFileWithMetadata` to expose Android content:// URIs.

## 0.0.2

* Adds `XFileWithLocalIdentifier` to expose iOS Photos identifiers.

## 0.0.1

* Initial release of `image_picker_plus_platform_interface`.
* Forked from `image_picker_platform_interface` v2.11.1.
* Renamed package from `image_picker_platform_interface` to `image_picker_plus_platform_interface`.
